# cloud-init-nginx-demo
This project demonstrates infrastructure as code (IaC) using Terraform by provisioning AWS EC2 instances and configuring them to run Nginx automatically at first boot using cloud-init.
